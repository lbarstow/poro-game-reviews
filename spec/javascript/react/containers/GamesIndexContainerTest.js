import GamesIndexContainer from '../../../../app/javascript/react/containers/GamesIndexContainer.js'
import GameTile from '../../../../app/javascript/react/components/GameTile.js'
//import fetch from 'react-fetch'

describe('GamesIndexContainer', () => {
  let wrapper;
  // console.log(responsePromise)

  //beforeEach(() => {
    // let data = {
    //   games: [
    //     {
    //       name: 'Catan',
    //       description: 'play games'
    //     }
    //   ]
    // }
    //
    // let responseBody = JSON.stringify(data);
    // let response = new Response(responseBody, {
    //   status: 200,
    //   statusText: 'OK',
    //   headers: { 'Content-Type': 'application/json' }
    // });
    // let responsePromise = Promise.resolve(response);
    // spyOn(global, 'fetch').and.returnValue(responsePromise);

  //   wrapper = mount(
  //     <GamesIndexContainer />
  //   )
  // })

  // it('should have a GameTile component', () => {
  //   // console.log(wrapper.state().games)
  //   expect(wrapper.find(GameTile)).toBePresent()
  // })

})
